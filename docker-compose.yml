# version: '3.1'

# services:
#   app:
#     build: .
#     image: azko-admin-image
#     container_name: azko-admin-front
#     restart: always
#     networks:
#       - proxynet

# networks:
#   proxynet:
#     external: true

services:
  azko-admin:
    image: $IMAGE_NAME:$IMAGE_TAG
    container_name: azko-admin
    restart: always
    networks:
      proxynet:

networks:
  proxynet:
    external: true
    name: proxynet
