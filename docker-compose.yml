version: '3.1'

services:
  app:
    build: .
    image: afmpro-admin-image
    container_name: afmpro-admin-front
    restart: always
    networks:
      - proxynet

networks:
  proxynet:
    external: true
