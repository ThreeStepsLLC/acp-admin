version: '3.1'

services:
  app:
    build: .
    image: his-admin-image
    container_name: his-admin
    restart: always
    ports:
      - "3002:80"
    networks:
      - proxynet

networks:
  proxynet:
    external: true