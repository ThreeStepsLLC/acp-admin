services:
  azko-admin:
    image: ${IMAGE_NAME}:${IMAGE_TAG} 
    container_name: azko-admin
    restart: always
    ports:
       - "127.0.0.1:3003:80"
    networks:
      - proxynet

networks:
  proxynet:
    external: true